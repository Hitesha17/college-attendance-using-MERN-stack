# Dockerfile generated by DevOpsArk

# Use the specified Node.js version from package.json engines
# Since engines.node is not provided, default to Node.js 20
FROM node:20-slim AS builder

# Set the working directory
WORKDIR /app

# Copy package.json and package-lock.json first for dependency installation
COPY package.json package-lock.json ./

# Install dependencies
RUN npm install --omit=dev

# Copy the rest of the application files
COPY . .

# Start the application using the specified start script
CMD ["npm", "start"]